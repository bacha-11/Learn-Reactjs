export function Logout(props){
    return(
        <div>
            <h1>
                Logout component 
            </h1>
            <button onClick={props.data}>get data</button>
            
        </div>
    )
}